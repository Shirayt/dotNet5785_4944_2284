   <Window x:Class="PL.Volunteer.VolunteerCallHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL"
        xmlns:conv="clr-namespace:PL"
        Title="Call History" Height="500" Width="900"
        Loaded="Window_Loaded"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Window.Resources>
        <local:FilterAndSortByFieldsCollection x:Key="SortOptionsCollection"/>
        <conv:StatusToColorConverter x:Key="StatusToColorConverter"/>
    </Window.Resources>

    <DockPanel Margin="10">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
            <TextBlock Text="Sort by:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox ItemsSource="{StaticResource SortOptionsCollection}"
                      SelectedItem="{Binding SortField}" Width="130" Margin="5"/>
            <TextBlock Text="Filter by:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox ItemsSource="{StaticResource SortOptionsCollection}"
                      SelectedItem="{Binding FilterField}" Width="130" Margin="5"/>
            <Button Content="Apply" Click="Apply_Click" Margin="10,0"/>
        </StackPanel>

        <ListView ItemsSource="{Binding ClosedCalls}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background"
                            Value="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Type" Width="100" DisplayMemberBinding="{Binding CallType}"/>
                    <GridViewColumn Header="Address" Width="200" DisplayMemberBinding="{Binding FullAddress}"/>
                    <GridViewColumn Header="Opened" Width="120" DisplayMemberBinding="{Binding OpenTime}"/>
                    <GridViewColumn Header="Started" Width="120" DisplayMemberBinding="{Binding TreatmentStartTime}"/>
                    <GridViewColumn Header="Ended" Width="120" DisplayMemberBinding="{Binding EndTime}"/>
                    <GridViewColumn Header="Status" Width="100" DisplayMemberBinding="{Binding Status}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
