<Window x:Class="PL.Volunteer.ChooseCallForTreatmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL"
        xmlns:conv="clr-namespace:PL"
        Title="Choose Call" Height="500" Width="900"
        Loaded="Window_Loaded"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Window.Resources>
        <local:FilterAndSortByFieldsCollection x:Key="SortOptionsCollection"/>
        <conv:CallTypeToColorConverter x:Key="CallTypeToColorConverter"/>
    </Window.Resources>

    <DockPanel Margin="10">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
            <TextBlock Text="Sort by:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox ItemsSource="{StaticResource SortOptionsCollection}"
                      SelectedItem="{Binding SortField}" Width="130" Margin="5"/>
            <Button Content="Apply" Click="Apply_Click" Margin="10,0"/>
        </StackPanel>

        <ListView ItemsSource="{Binding OpenCalls}" SelectedItem="{Binding SelectedCall}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background"
                 Value="{Binding CallType, Converter={StaticResource CallTypeToColorConverter}}"/>
                </Style>
            </ListView.ItemContainerStyle>
            
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding CallType}" Width="100"/>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="200"/>
                    <GridViewColumn Header="Address" DisplayMemberBinding="{Binding FullAddress}" Width="200"/>
                    <GridViewColumn Header="Opened" DisplayMemberBinding="{Binding OpenTime}" Width="120"/>
                    <GridViewColumn Header="Distance" DisplayMemberBinding="{Binding DistanceFromVolunteer, StringFormat={}{0:F1} km}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Content="Select Call"
                DockPanel.Dock="Bottom" HorizontalAlignment="Right" Margin="10"
                Click="Select_Click"/>
    </DockPanel>
</Window>
